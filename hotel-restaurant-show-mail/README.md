# ホテル・レストラン・ショー フォローメール 下書き作成ツール（GAS）

スプレッドシートの名刺データ（社名・氏名・メールアドレス）を差し込み、
**Gmail に下書き（Draft）を作成**する Google Apps Script です。
※ 送信はしません。作成後に Gmail の「下書き」で内容を確認してから送ってください。

- **差出人**: 手間いらず株式会社 石束（挨拶・署名は `CONFIG.SENDER` で管理）
- **CC**: `biz@temairazu.com` を全件に付与
- **件名**: `ホテル・レストラン・ショー御礼と打ち合わせのお願い`（`CONFIG.SUBJECT` で変更可）
- **会社名の差し込み**: 法人名（D列）を優先、なければ施設名（B列）
- **名前の差し込み**: 姓（H列）＋ 名（I列）
- **メールアドレス空欄の行**: スキップし、実行後のログ／ダイアログに一覧表示

> ⚠️ **列の並びについて**: 石束さんのリスト（`担当者マスタ`）は、データがヘッダーより
> **1列左**にずれています（施設名=B列・法人名=D列・姓=H列・名=I列・メール=O列）。
> そのため列は「見出し名」ではなく **固定の列文字**（`CONFIG.COL_*`）で指定しています。
> 別レイアウトのシートに使う場合は `CONFIG.COL_*` の列文字を変えるだけでOKです。

## セットアップ

1. 元の Excel ファイルを Google スプレッドシートとして開く（または取り込む）。
   - データは6行目から。列は `CONFIG.COL_*`（列文字）で指定します。
2. スプレッドシート上部メニュー **拡張機能 → Apps Script** を開く。
3. `コード.gs` の中身を貼り付けて保存。
4. スプレッドシートに戻り、**再読み込み**するとメニュー **「メール下書き」** が表示される。

## 使い方

1. **メール下書き → ① プレビュー（下書きは作らない）**
   - 差し込み結果（宛先・会社名・名前）と、対象/スキップ件数を確認。
2. 問題なければ **メール下書き → ② Gmail下書きを作成**
   - 全件の下書きが作成される。Gmail の「下書き」フォルダで最終確認 → 送信。

初回実行時に Gmail / スプレッドシートへのアクセス許可（承認）を求められます。
実行アカウント自身の Gmail に下書きが作成されます。

## 設定（`CONFIG`）

| 項目 | 既定値 | 説明 |
|------|--------|------|
| `SHEET_NAME` | `''`（空=アクティブシート） | 対象シート名 |
| `DATA_START_ROW` | `6` | データ開始行（テストデータ・注釈行の次） |
| `CC` | `biz@temairazu.com` | 全件に付与するCC |
| `SUBJECT` | 上記件名 | メール件名 |
| `COL_FACILITY` | `'B'` | 施設名の列 |
| `COL_CORP` | `'D'` | 法人名の列 |
| `COL_LAST` | `'H'` | 姓の列 |
| `COL_FIRST` | `'I'` | 名の列 |
| `COL_EMAIL` | `'O'` | メールアドレスの列 |

列は **見出し名ではなく列文字**（`CONFIG.COL_*`）で指定します。
別レイアウトのシートに使う場合は、この列文字を実データの入っている列に合わせてください。

## 差し込み対象（`担当者マスタ` の場合）

全 **9 件**が下書き対象です（全員メールアドレスあり、スキップ0件）。

| 会社名 | 名前 | メール |
|--------|------|--------|
| 株式会社 南西観光 | 上原 尚彦 | uehara.n@nansei-kanko.com |
| アール・ケイ・アミューズメント(株) | 上地 宰 | mail.uechi-ts@rka.bz |
| (株)シーサー／沖縄 | 福田 順一郎 | fukuda@seasir.com |
| ハレクラニ沖縄 | 佐々木 亮子 | ryoko.sasaki@halekulanitokinaowa.com |
| KARIYUSHI HOTELS | 丹生 眞沙美 | m-nio@kariyushi-beach.co.jp |
| 株式会社ファイブスターコーポレーション | 神谷 笑美 | e-kamiya@fivestar-corporation.co.jp |
| 合同会社 サザンプロジェクト | 川口 達也 | kawaguchi@southern-project.co.jp |
| HOTEL PEACE ISLAND NAGO | 山畑 卓也 | yamahata@peace-k.jp |
| ジィ・エイチ・エス沖縄 株式会社 | 名嘉 利勇 | nakatoshiyuki@ghs-okinawa.jp |

> 会社名は「法人名優先・なければ施設名」です。上表のうち「KARIYUSHI HOTELS」
> （施設名：KARIYUSHI LCH. Resort on the Beach）と「HOTEL PEACE ISLAND NAGO」
> （施設名：ホテルピースアイランド名護）は、法人名が英語表記のため英語社名を採用しています。
> 日本語の施設名を使いたい場合は `COL_CORP` と `COL_FACILITY` の優先順を入れ替えてください。
